services:
  yt-fabric-ui:
    build: .
    image: yt-fabric-ui:latest
    network_mode: "host"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./.config/fabric:/home/fabric/.config/fabric
    environment:
      - PORT=8090
